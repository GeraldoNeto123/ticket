{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/runbook-checkpoint.py",
            "timeout": 20,
            "statusMessage": "Conferindo o checkpoint do runbook..."
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/referencias-de-linha.py",
            "timeout": 10,
            "statusMessage": "Conferindo referências de linha no documento..."
          }
        ]
      }
    ]
  }
}
